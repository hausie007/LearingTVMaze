# Default Theme

Place PNG images with transparent backgrounds in this directory:

- `start.png`  – shown on the Start cell (bottom-left corner)
- `end.png`    – shown on the End / goal cell (top-right corner)
- `player.png` – the player character sprite

Images are scaled to fit inside a cell. If a file is missing, the game uses a coloured square fallback.
